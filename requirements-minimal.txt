# Minimal requirements for API deployment (reduces memory usage)
# Use this for Render free tier deployment

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Core data science libraries (required for model)
pandas>=2.0.0
numpy>=1.24.0

# Machine learning (required for model)
scikit-learn>=1.3.0

# Model persistence (required)
joblib>=1.3.0

# Removed to save memory:
# - matplotlib/seaborn (not needed for API)
# - xgboost/lightgbm/catboost (not needed if using RandomForest)
# - optuna (only needed for training)
# - openpyxl (only needed for data processing)
# - tqdm (only needed for scripts)

