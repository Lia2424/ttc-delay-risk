# Minimal requirements for API deployment (reduces memory usage)
# Optimized for Render free tier (512MB limit)

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Core data science libraries (required for model)
pandas>=2.0.0
numpy>=1.24.0

# Machine learning (required for model)
scikit-learn>=1.3.0

# Model persistence (required)
joblib>=1.3.0

# Removed to save memory (~100-200MB):
# - matplotlib/seaborn (not needed for API, only for visualization)
# - xgboost/lightgbm/catboost (not needed if using RandomForest model)
# - optuna (only needed for training, not inference)
# - openpyxl (only needed for data processing scripts)
# - tqdm (only needed for scripts, not API)
